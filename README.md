# ENS Network For Unity

The protocol module is used to wrap api such as socket, tcpclient, udpclient or blueteeth api in the future. This enables you to use it in special platforms which has limits on data transform like wechat mini games.
